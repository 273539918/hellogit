【du命令和df命令】：
du和df 的区别：
1、du是遍历所有还存在的文件来统计大小; df 是统计父一级superblock来统计大小。
2、du无法统计已经删除但还有进程在引用的文件 ； df会统计到已经删除但进程还在引用的文件
问题：什么情况du比df大？
答：都查看根大小时，du比df大。根的一个子目录是一个单独文件系统，这部分发小就不会被df统计到根上（superblock)，而du会（遍历文件）
问题：什么情况df比du大
答：统计的范围内有文件被删除但还有进程在引用 。 可以通过lsof | grep deleted 查看